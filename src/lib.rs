//! asciiquarium engine, exposed as a library so the binary and the integration
//! tests share the same compositor, entity model, art, and spawners.

pub mod art;
pub mod entity;
pub mod render;
pub mod spawn;
